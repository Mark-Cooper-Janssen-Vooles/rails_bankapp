<%= form_with(model: bank_detail, local: true) do |form| %>
  <% if bank_detail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(bank_detail.errors.count, "error") %> prohibited this bank_detail from being saved:</h2>

      <ul>
      <% bank_detail.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :bsb %>
    <%= form.text_field :bsb %>
  </div>

  <div class="field">
    <%= form.label :account_number %>
    <%= form.text_field :account_number %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
